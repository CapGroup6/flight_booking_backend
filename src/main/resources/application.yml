spring:
  application:
    name: capstone-flight-booking
  profiles:
    active: test
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: PST
  main:
    allow-bean-definition-overriding: true

server:
  port: 8080
  servlet:
    session:
      timeout: 1800
      cookie:
        name: JSESSIONID
